<p><%= t('devise.email.welcome') + @email %>!</p>

<p><%= t('devise.email.confirmation_instructions.1') %></p>

<p><%= link_to t('devise.email.confirmation_instructions.link'), confirmation_url(@resource, confirmation_token: @token) %></p>

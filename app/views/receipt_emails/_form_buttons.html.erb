<% if !@model.new_record? %>
	<%= link_to t('helpers.action.email.resend'), 'javascript:void(0)', :class => "btn btn-primary resend_btn" %>
	<%= link_to t('helpers.action.email.send_test'), 'javascript:void(0)', :class => "btn btn-primary send_test_btn" %>
<% end %>
<%# form_tag '/donations/filter' do %>
    <!-- <div class="row">
        <div class="col-md-6 form-group">
            <%= label_tag "Mantenedora" %>
            <%= select_tag("search[company_id]", options_from_collection_for_select(Company.where(group: 1), "id", "trading_name", @company_id), {:include_blank => genderize_full_tag(Company.new, 'helpers.select.all'), class: "form-control"}) %>
        </div>

        <div class="col-md-6 form-group">
            <%= label_tag "Mês" %>
            <%= select_tag("search[month]", options_for_select(@month_list, @month), {:include_blank => genderize_full_tag(Email.new, 'helpers.select.all'), class: "form-control"}) %>
        </div>
    </div> -->

    <%= link_to genderize_full_tag(@model, 'helpers.action.new'), new_donation_path, :class => "btn btn-primary" %>
    <!--<%= submit_tag t('helpers.action.filter'), class: "btn btn-primary" %> -->
<%# end %>

</p>
</p>


<table class="table table-striped table-hover">
    <tr>
        <% I18n.with_options(scope: 'activerecord.attributes.donation') do |i18n| %>
            <th style="width: 16px; display: none;"></th>
            <th style="width: 16px;"><%= i18n.t :donation_date %></th>
            <th><%= i18n.t :value %></th>
            <th><%= i18n.t :company %></th>
            <th class="hidden-xs" ><%= i18n.t :remark %></th>
        <% end %>
    </tr>

    <% @list.each do |iterator| %>
        <tr id='model_<%= iterator.id %>'>
                <td style="width: 16px; display: none;" class="model_id"><%= iterator.id %></td>
                <td style="width: 16px;"><%= iterator.donation_date %></td>
                <td><%= number_to_currency(iterator.value) %></td>
                <td class="email_company"><%= iterator.company.trading_name %></td>
                <td class="hidden-xs" ><%= iterator.remark %></td>
        </tr>
    <% end %>
</table>

<%= render partial: "helpers/index_helper", locals: {model: @model} %>

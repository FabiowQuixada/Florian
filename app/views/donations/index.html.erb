<%# form_tag '/donations/filter' do %>
    <!-- <div class="row">
        <div class="col-md-6 form-group">
            <%= label_tag "Mantenedora" %>
            <%= select_tag("search[company_id]", options_from_collection_for_select(Company.where(group: 1), "id", "trading_name", @company_id), {:include_blank => genderize_full_tag(Company.new, 'helpers.select.all'), class: "form-control"}) %>
        </div>

        <div class="col-md-6 form-group">
            <%= label_tag "Mês" %>
            <%= select_tag("search[month]", options_for_select(@month_list, @month), {:include_blank => genderize_full_tag(ReceiptEmail.new, 'helpers.select.all'), class: "form-control"}) %>
        </div>
    </div> -->

    <!--<%= submit_tag t('helpers.action.filter'), class: "btn btn-primary" %> -->
<%# end %>

<%= render partial: "helpers/index_table", locals: {model: @model, columns: Array.[](
    Hash[:header => "donation_date", :model_attr => 'donation_date', :style => "width: 16px;"],
    Hash[:header => "value", :model_attr => 'value', :style => "width: 150px;", :type => 'money'],
    Hash[:header => "company", :model_attr => 'company.trading_name'],
    Hash[:header => "remark", :model_attr => 'remark']
)} %>

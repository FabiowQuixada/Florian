<nav class="navbar navbar-default">
    <div class="navbar-header inline-block">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#main_menu">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" rel="home" href="/" title="<%= t('app_title') %>">
            <span><img style="max-width: 100%; max-height: 150%; margin-top: -5px;" alt="<%= t('app_title') %>" src="/assets/app_logo.png"></span>
            <span class="app_title"><%= t('app_title') %></span>
        </a>
    </div>
    <div class="collapse navbar-collapse" id="main_menu">
        <ul class="nav navbar-nav">
            <%= menu Company %>
            <%= menu Donation %>
            <li class="dropdown">
                <a class="dropdown-toggle" data-toggle="dropdown" href="javascript:void(0)"><%= t('menu.emails') %></a>
                <ul class="dropdown-menu">
                    <%= menu ReceiptEmail, t('menu.email.receipt') %>
                    <%= menu ProductAndServiceDatum %>
                </ul>
            </li>
            <%= menu Bill %>

            <% if !current_user.nil? && current_user.admin? %>
                <%= menu User %>
                <%= menu Role %>
            <% end %>
            <li class="visible-xs">
                <%= content_tag :a, "#{current_user.name} (#{t('helpers.action.logout')})", href: edit_user_registration_path %>
            </li>
        </ul>
    </div>
</nav>

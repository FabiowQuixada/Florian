<nav class="navbar navbar-default">
    <div class="navbar-header inline-block">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#main_menu">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" rel="home" href="/" title="<%= t('app_title') %>">
            <span><img style="max-width: 100%; max-height: 150%; margin-top: -5px;" alt="<%= t('app_title') %>" src="/assets/app_logo.png"></span>
            <span class="app_title"><%= t('app_title') %></span>
        </a>
    </div>
    <div class="collapse navbar-collapse" id="main_menu">
        <ul class="nav navbar-nav">
            <%= render partial: 'others/menu_item', locals: { klass: Company } %>
            <%= render partial: 'others/menu_item', locals: { klass: Donation } %>
            <li class="dropdown">
                <a class="dropdown-toggle" data-toggle="dropdown" href="javascript:void(0)"><%= t('menu.emails') %></a>
                <ul class="dropdown-menu">
                    <%= render partial: 'others/menu_item', locals: { klass: ReceiptEmail, display_name: t('menu.email.receipt') } %>
                    <%= render partial: 'others/menu_item', locals: { klass: ProductAndServiceDatum } %>
                </ul>
            </li>
            <%= render partial: 'others/menu_item', locals: { klass: Bill } %>

            <% if !current_user.nil? && current_user.admin? %>
                <%= render partial: 'others/menu_item', locals: { klass: User } %>
                <%= render partial: 'others/menu_item', locals: { klass: Role } %>
            <% end %>
            <li class="visible-xs">
                <%= content_tag :a, "#{current_user.name} (#{t('helpers.action.logout')})", href: edit_user_registration_path %>
            </li>
        </ul>
    </div>
</nav>

require 'rails_helper'

describe 'General', type: :request do
  it 'Access the root path with no error message if already logged in' do
    # login_as_admin

    # visit root_path

    # expect(page).to have_no_content('já está')
  end
end
